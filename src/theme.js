import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      dark: '#b2a300',
      main: '#ffeb3b',
      light: '#ffee33'
    },
    secondary: {
      dark: '#1769aa',
      main: '#00b0ff',
      light: '#4dabf5'
    },
  },
})

export default theme
